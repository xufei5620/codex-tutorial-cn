# 用法：在仓库根目录执行 docker compose -f deploy/docker-compose.yml up -d
# 然后浏览器打开 http://服务器IP:8080
services:
  codex-tutorial:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    container_name: codex-tutorial
    ports:
      - "8080:80"
    restart: unless-stopped
