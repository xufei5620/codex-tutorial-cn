# 证据目录

- 结构：`windows/` 与 `macos/` 两个子目录；文件名以测试项 ID 开头，如 `T03-06-permission-dropdown.png`、`T04-01-reply.txt`。
- 脱敏后再放进来：不得出现邮箱、账号名、头像、真实姓名、工作区名、路径里的用户名。优先截局部（弹窗、菜单、下拉）。
- 这里的文件只作维护证据，不进读者页面，也不进离线 ZIP（整个 `src/` 都不进）。
